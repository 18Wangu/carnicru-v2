import { Days_One } from "next/font/google";
import { Montserrat } from "next/font/google";

export const daysOneFont = Days_One({
    weight: "400",
    subsets: ["latin"],
});

export const montserratFont = Montserrat({
    weight: "600",
    subsets: ["latin"],
});
